/**
 * @file utils.js
 * @fileoverview This file contains utility functions for the app.
 */

/**
 * Generates a unique ID based on the current timestamp.
 * @returns {string} a unique ID
 */
export const generateID = () => Date.now().toString();
